<div class="max-w-7xl mx-auto px-6 py-8">
    <div class="flex justify-between items-center mb-6">
        <h2 class="text-2xl font-bold text-white">Clientes</h2>
        <button onclick="document.getElementById('new-client-modal').classList.remove('hidden')" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition-colors flex items-center">
            <i class="ph ph-plus mr-2"></i> Nuevo Cliente
        </button>
    </div>

    <div class="card p-6">
        <% if (typeof clientes !== 'undefined' && clientes && clientes.length > 0) { %>
            <div class="overflow-x-auto">
                <table class="w-full text-left border-collapse">
                    <thead>
                        <tr class="text-slate-400 border-b border-slate-700">
                            <th class="py-3 px-4">Nombre</th>
                            <th class="py-3 px-4">Empresa</th>
                            <th class="py-3 px-4">Fecha Registro</th>
                            <th class="py-3 px-4">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% clientes.forEach(cliente => { %>
                            <tr class="border-b border-slate-800 hover:bg-slate-900/50">
                                <td class="py-3 px-4 font-medium text-white"><%= cliente.nombre %></td>
                                <td class="py-3 px-4 text-slate-300"><%= cliente.empresa || '-' %></td>
                                <td class="py-3 px-4 text-slate-400"><%= new Date(cliente.creado_en).toLocaleDateString() %></td>
                                <td class="py-3 px-4">
                                    <a href="/clientes/<%= cliente.id %>" class="text-blue-400 hover:text-blue-300 mr-2"><i class="ph ph-eye"></i></a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="text-center py-12">
                <div class="text-slate-500 mb-4 text-5xl"><i class="ph ph-users"></i></div>
                <p class="text-slate-400 text-lg">No hay clientes registrados.</p>
                <p class="text-slate-500 text-sm">Agrega un nuevo cliente para comenzar.</p>
            </div>
        <% } %>
    </div>
</div>

<!-- Modal Nuevo Cliente -->
<div id="new-client-modal" class="fixed inset-0 bg-black/50 hidden flex items-center justify-center z-50">
    <div class="bg-slate-800 p-6 rounded-xl w-full max-w-md shadow-2xl border border-slate-700">
        <h3 class="text-xl font-bold text-white mb-4">Nuevo Cliente</h3>
        <form action="/clientes" method="POST">
            <div class="mb-4">
                <label class="block text-slate-400 text-sm mb-2">Nombre</label>
                <input type="text" name="nombre" required class="w-full bg-slate-900 border border-slate-700 rounded p-2 text-white focus:outline-none focus:border-blue-500">
            </div>
            <div class="mb-4">
                <label class="block text-slate-400 text-sm mb-2">Empresa</label>
                <input type="text" name="empresa" class="w-full bg-slate-900 border border-slate-700 rounded p-2 text-white focus:outline-none focus:border-blue-500">
            </div>
            <div class="flex justify-end gap-3 mt-6">
                <button type="button" onclick="document.getElementById('new-client-modal').classList.add('hidden')" class="px-4 py-2 text-slate-300 hover:text-white">Cancelar</button>
                <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded">Guardar</button>
            </div>
        </form>
    </div>
</div>
